# Videos antigos primeiro
--playlist-reverse

# Pasta de download dos vídeos
# Linux
#-P '/home/dudu/Porno/'

# Windows
-P 'B:\'

# Saida do arquivo de vídeo
# Linux
#-o "%(extractor)s/%(channel)s/%(playlist_autonumber|)03d%(playlist_index& - |)s%(playlist_title|)s%(playlist_title& - |)s%(title)s.%(ext)s"
#-o "%(extractor)s/%(channel)s/%(playlist_title|)s/%(playlist_autonumber|)03d%(playlist_title& - |)s%(title)s.%(ext)s"

# Windows
-o "%(extractor)s\%(channel)s\%(playlist_title|)s\%(playlist_autonumber|)03d%(playlist_title& - |)s%(title)s.%(ext)s"

# Baixar apenas videos que tenha a palavra especifica
--match-filter "!is_live & title ~= 'Varadouro'"

# Baixar arquivo para nao reescrever:
#--download-archive "%(extractor)s\%(channel)s\%(playlist_title|)s\archive.txt"